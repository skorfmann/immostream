import React from 'react';
import Helmet from 'react-helmet';
import config from '../../../config/SiteConfig';

const SEO = (props) => {
  return (
    <Helmet>
      <meta name="twitter:image" content="yeah" />
    </Helmet>
  );
};

export default SEO;
